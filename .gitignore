# LaTeX build artifacts
build/
*.aux
*.log
*.out
*.toc
*.ptc
*.tmp
*.fls
*.fdb_latexmk
*.synctex.gz
*.nav
*.snm
*.vrb

# CI builds the notebook and publishes it as a release, so it is not committed.
notebook.pdf
